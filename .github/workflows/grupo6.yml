# 1. El nombre del workflow es grupo6
name: grupo6

# 2. Se activa por push en la rama principal o por botón 
on:
  push:
    branches:
      - main
  workflow_dispatch:

# 3. Las tres variables de entorno
env:
  VARIABLE1: "valor1"
  VARIABLE2: "valor2"
  VARIABLE3: "valor3"
